whoami - выводит имя пользователя, под которым выводится текущая команда;
id - выводит информацию о текущем пользователе и группах, в которых он состоите);
groups - выводит все группы, в которых состоит текущий пользователь;
visudo - позволяет редактировать конфигурационный файл /etc/sudoers с возможностью отката;

/etc/sudoers

{кому выдаем права} {имя хоста}=({права на запуск из под пользователя\группы}) {без запароса пароля}: {на что выдаем права}

#Вася получает полные права, как администратор, запуск любых команд из под любого пользователя
vasya ALL=(ALL:ALL) ALL

#Вася получает полные права, но они будут работать только если имя сервера server1, если эту настройку скопировать на другой сервер, где hostname будет my_server, то эта директива не будет работать
vasya server1=(ALL:ALL)  ALL

#Вася получает права на запуск любой команды, но из под определенного пользователя
vasya server1=(user1:user1)  ALL

#чуть более реальная ситуация, создали обычного пользователя, у него нет прав на запуск утилиты tcpdump, а давать права полные на сервер нельзя

vasya ALL=(ALL) NOPASSWD: /usr/sbin/tcpdump


#Если надо в промышленных маштабах выдавать права на tcpdump, выдаем права группе network, после чего добавляем нужных пользователей в группу, а когда им больше не нужны права, просто удаляем у пользователя данную группу, права при этом выданы на группу и каждый раз не нужно их править, или делатьм 50 одинаковых прав для каждого пользователя.

%network ALL=(ALL) NOPASSWD: /usr/sbin/tcpdump

